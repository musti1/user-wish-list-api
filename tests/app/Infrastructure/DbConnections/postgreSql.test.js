const { expect } = require('chai');
const { it, describe } = require('mocha');

const UserRepository = require('../../../../app/Infrastructure/DbConnections/postgreSql');

describe('', () => {
    it('', async () => {

        console.log(UserRepository);
    })
});
